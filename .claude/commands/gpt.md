지금까지의 대화 맥락을 간결하게 요약하고, 사용자의 질문 "$ARGUMENTS"와 합쳐서 하나의 프롬프트를 구성한 다음, 아래 명령어로 GPT에 전달하고 그 결과를 그대로 출력하세요.

```bash
python3 ~/.claude/scripts/ask_gpt.py "<구성한 프롬프트>"
```

출력 형식:
- 먼저 `**[GPT-4o 답변]**` 헤더를 표시하세요.
- 그 아래에 GPT의 답변을 그대로 출력하세요.
- 마지막에 한 줄 구분선 후 Claude의 짧은 코멘트(선택)를 추가해도 됩니다.
